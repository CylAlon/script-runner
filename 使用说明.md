# Script Executor 使用说明

## 简介

Script Executor 是一个方便快捷的 VSCode 插件，允许你在编辑器中快速执行自定义脚本命令。

### 核心特性

- ✅ **内置常用命令**：包含 NPM 和 Git 最常用的操作，开箱即用
- ✅ **快速搜索**：支持模糊搜索，快速找到需要的脚本
- ✅ **两层配置**：支持全局配置和项目配置
- ✅ **智能合并**：自动去重，项目配置优先

## 快速开始

### 1. 安装插件

在 VSCode 扩展市场搜索 "Script Executor" 并安装，或者从本地安装 `.vsix` 文件。

### 2. 使用内置命令（可选配置）

**插件默认启用了内置命令**，安装后即可直接使用常用的 NPM 和 Git 命令，无需配置！

包含以下内置命令：
- **NPM**: install, start, build, test, dev, lint
- **Git**: status, pull, push, commit, add, log

### 3. 添加自定义脚本（可选）

如果需要添加项目特定的脚本，可以配置：
- **全局配置**：适用于所有项目的通用脚本
- **项目配置**：仅适用于当前项目的专属脚本

### 4. 使用脚本

点击编辑器右上角的 **终端图标** 或使用命令面板（`Cmd/Ctrl + Shift + P`）搜索 **"打开脚本执行器"**，然后：
- 输入关键词快速搜索（如 "install", "git", "build"）
- 选择要执行的脚本即可

---

## 配置详解

### 配置项 1：`scriptExecutor.enableBuiltinScripts`

是否启用内置的常用命令（默认：`true`）

```json
{
  "scriptExecutor.enableBuiltinScripts": true
}
```

- 设置为 `true`：显示内置的 NPM 和 Git 常用命令
- 设置为 `false`：只显示自定义脚本

**内置命令列表**：

**NPM 命令**：
- `$(package) NPM: 安装依赖` - npm install
- `$(play) NPM: 启动项目` - npm start
- `$(tools) NPM: 构建项目` - npm run build
- `$(beaker) NPM: 运行测试` - npm test
- `$(rocket) NPM: 开发模式` - npm run dev
- `$(check) NPM: 代码检查` - npm run lint

**Git 命令**：
- `$(git-branch) Git: 查看状态` - git status
- `$(sync) Git: 拉取代码` - git pull
- `$(repo-push) Git: 推送代码` - git push
- `$(git-commit) Git: 提交暂存` - git commit
- `$(add) Git: 添加所有` - git add .
- `$(history) Git: 查看日志` - git log --oneline -10

---

### 配置项 2：`scriptExecutor.scripts`

这是插件的核心配置项，用于定义自定义脚本列表。

#### 配置类型

```typescript
{
  "scriptExecutor.scripts": [
    {
      "label": string,        // 必填：脚本的显示名称
      "description": string,  // 可选：脚本的描述信息
      "command": string       // 必填：要执行的终端命令
    }
  ]
}
```

#### 参数说明

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `label` | string | ✅ 是 | 脚本在选择菜单中显示的名称。支持使用 VSCode 图标，如 `$(terminal)` `$(rocket)` 等 |
| `description` | string | ❌ 否 | 脚本的描述信息，会显示在选择菜单的右侧，帮助识别脚本用途 |
| `command` | string | ✅ 是 | 实际要在终端中执行的命令，支持任何 shell 命令 |

---

## 全局配置

全局配置适用于所有项目，通常用于配置通用的开发工具命令。

### 配置位置

**macOS/Linux**: `~/Library/Application Support/Code/User/settings.json`
**Windows**: `%APPDATA%\Code\User\settings.json`

或者通过 VSCode 打开：
1. 按 `Cmd/Ctrl + ,` 打开设置
2. 点击右上角的 **打开设置 (JSON)** 图标
3. 在打开的文件中添加配置

### 配置示例

```json
{
  "scriptExecutor.scripts": [
    {
      "label": "$(terminal) Git Status",
      "description": "查看 Git 仓库状态",
      "command": "git status"
    },
    {
      "label": "$(sync) Git Pull",
      "description": "拉取最新代码",
      "command": "git pull"
    },
    {
      "label": "$(repo-push) Git Push",
      "description": "推送代码到远程仓库",
      "command": "git push"
    },
    {
      "label": "$(package) NPM Install",
      "description": "安装项目依赖",
      "command": "npm install"
    },
    {
      "label": "$(trash) 清理缓存",
      "description": "删除 node_modules 和重新安装",
      "command": "rm -rf node_modules && npm install"
    },
    {
      "label": "$(server) 查看端口占用",
      "description": "查看 3000 端口占用情况",
      "command": "lsof -i :3000"
    }
  ]
}
```

---

## 项目配置

项目配置仅适用于当前项目，通常用于配置项目特定的构建、测试、启动命令。

### 配置位置

在项目根目录下的 `.vscode/settings.json` 文件中配置。

如果该文件不存在，需要先创建：
1. 在项目根目录创建 `.vscode` 文件夹（如果不存在）
2. 在 `.vscode` 文件夹中创建 `settings.json` 文件
3. 添加配置

### 配置示例 1：前端项目

```json
{
  "scriptExecutor.scripts": [
    {
      "label": "$(rocket) 启动开发服务器",
      "description": "运行 Vite 开发服务器",
      "command": "npm run dev"
    },
    {
      "label": "$(tools) 构建生产版本",
      "description": "构建用于生产环境的代码",
      "command": "npm run build"
    },
    {
      "label": "$(eye) 预览生产版本",
      "description": "预览构建后的生产版本",
      "command": "npm run preview"
    },
    {
      "label": "$(beaker) 运行测试",
      "description": "执行单元测试",
      "command": "npm run test"
    },
    {
      "label": "$(check-all) 代码检查",
      "description": "运行 ESLint 检查代码",
      "command": "npm run lint"
    },
    {
      "label": "$(wrench) 修复代码",
      "description": "自动修复 ESLint 错误",
      "command": "npm run lint -- --fix"
    }
  ]
}
```

### 配置示例 2：后端 Node.js 项目

```json
{
  "scriptExecutor.scripts": [
    {
      "label": "$(play) 启动服务",
      "description": "启动 Express 服务器",
      "command": "npm start"
    },
    {
      "label": "$(debug-alt) 调试模式",
      "description": "使用 nodemon 启动开发模式",
      "command": "npm run dev"
    },
    {
      "label": "$(database) 数据库迁移",
      "description": "运行数据库迁移脚本",
      "command": "npm run migrate"
    },
    {
      "label": "$(refresh) 重置数据库",
      "description": "重置并重新创建数据库",
      "command": "npm run db:reset"
    },
    {
      "label": "$(beaker) 运行测试",
      "description": "执行测试套件",
      "command": "npm test"
    },
    {
      "label": "$(graph) 测试覆盖率",
      "description": "生成测试覆盖率报告",
      "command": "npm run test:coverage"
    }
  ]
}
```

### 配置示例 3：Python 项目

```json
{
  "scriptExecutor.scripts": [
    {
      "label": "$(play) 运行应用",
      "description": "启动 Flask 应用",
      "command": "python app.py"
    },
    {
      "label": "$(package) 安装依赖",
      "description": "使用 pip 安装依赖",
      "command": "pip install -r requirements.txt"
    },
    {
      "label": "$(beaker) 运行测试",
      "description": "使用 pytest 运行测试",
      "command": "pytest"
    },
    {
      "label": "$(database) 数据库迁移",
      "description": "运行 Alembic 迁移",
      "command": "alembic upgrade head"
    },
    {
      "label": "$(check) 代码格式化",
      "description": "使用 black 格式化代码",
      "command": "black ."
    }
  ]
}
```

### 配置示例 4：Docker 项目

```json
{
  "scriptExecutor.scripts": [
    {
      "label": "$(vm) 启动容器",
      "description": "使用 docker-compose 启动所有服务",
      "command": "docker-compose up -d"
    },
    {
      "label": "$(debug-stop) 停止容器",
      "description": "停止所有运行的容器",
      "command": "docker-compose down"
    },
    {
      "label": "$(refresh) 重建容器",
      "description": "重新构建并启动容器",
      "command": "docker-compose up -d --build"
    },
    {
      "label": "$(output) 查看日志",
      "description": "查看容器日志",
      "command": "docker-compose logs -f"
    },
    {
      "label": "$(trash) 清理资源",
      "description": "清理未使用的 Docker 资源",
      "command": "docker system prune -f"
    }
  ]
}
```

---

## 配置优先级

当同时存在全局配置和项目配置时，插件会合并这两份配置：

1. **项目配置优先显示**：项目配置的脚本会显示在列表顶部
2. **去重机制**：如果全局配置和项目配置中有相同的 `command`，只保留项目配置中的版本
3. **独立显示**：不同命令的脚本都会显示，即使 `label` 相同

### 示例

**全局配置**：
```json
{
  "scriptExecutor.scripts": [
    {
      "label": "$(terminal) 构建",
      "command": "npm run build"
    },
    {
      "label": "$(beaker) 测试",
      "command": "npm test"
    }
  ]
}
```

**项目配置**：
```json
{
  "scriptExecutor.scripts": [
    {
      "label": "$(rocket) 启动",
      "command": "npm run dev"
    },
    {
      "label": "$(tools) 项目构建",
      "command": "npm run build"
    }
  ]
}
```

**最终显示**（按顺序）：
1. ⭐ $(rocket) 启动 - 来自项目配置
2. ⭐ $(tools) 项目构建 - 来自项目配置
3. $(beaker) 测试 - 来自全局配置（`npm run build` 已在项目配置中，不重复显示）
4. ─────── 内置命令 ─────── - 分隔符
5. $(package) NPM: 安装依赖 - 内置命令
6. ... 其他内置命令

---

## 搜索功能

插件内置了强大的搜索功能，可以快速找到需要的脚本。

### 使用方法

1. 打开脚本执行器（点击右上角图标或使用命令面板）
2. 在弹出的选择框中直接输入关键词
3. 插件会自动过滤匹配的脚本

### 搜索范围

搜索会匹配以下内容：
- **脚本名称**（label）
- **描述信息**（description）
- **命令内容**（command）

### 搜索示例

| 输入关键词 | 会匹配到 |
|-----------|---------|
| `install` | NPM: 安装依赖, pip install 等 |
| `git` | 所有 Git 相关命令 |
| `build` | NPM: 构建项目, 以及所有包含 build 的自定义脚本 |
| `test` | NPM: 运行测试, pytest 等测试相关命令 |
| `npm` | 所有 NPM 相关命令 |
| `dev` | NPM: 开发模式, docker-compose 等 |

### 模糊搜索

搜索支持模糊匹配，例如：
- 输入 `gts` 可以匹配 `Git: 查看状态`
- 输入 `npi` 可以匹配 `NPM: 安装依赖`

---

## 常用图标参考

VSCode 支持丰富的内置图标，以下是常用图标列表：

| 图标代码 | 效果 | 适用场景 |
|---------|------|----------|
| `$(terminal)` | 终端 | 命令行操作 |
| `$(rocket)` | 火箭 | 启动、运行 |
| `$(tools)` | 工具 | 构建 |
| `$(beaker)` | 烧杯 | 测试 |
| `$(check)` | 对勾 | 检查、验证 |
| `$(wrench)` | 扳手 | 修复、配置 |
| `$(gear)` | 齿轮 | 设置 |
| `$(package)` | 包 | 安装依赖 |
| `$(database)` | 数据库 | 数据库操作 |
| `$(play)` | 播放 | 启动服务 |
| `$(debug-alt)` | 调试 | 调试模式 |
| `$(trash)` | 垃圾桶 | 清理、删除 |
| `$(refresh)` | 刷新 | 重置、重启 |
| `$(sync)` | 同步 | 拉取、同步 |
| `$(repo-push)` | 推送 | Git push |
| `$(vm)` | 虚拟机 | Docker、容器 |
| `$(server)` | 服务器 | 服务相关 |
| `$(eye)` | 眼睛 | 预览、查看 |
| `$(output)` | 输出 | 日志、输出 |

完整图标列表请访问：[VSCode Icons Reference](https://code.visualstudio.com/api/references/icons-in-labels)

---

## 使用技巧

### 1. 链式命令

可以在一个脚本中执行多个命令：

```json
{
  "label": "$(rocket) 完整部署",
  "description": "测试、构建并部署",
  "command": "npm test && npm run build && npm run deploy"
}
```

### 2. 带参数的命令

```json
{
  "label": "$(server) 指定端口启动",
  "description": "在 8080 端口启动服务",
  "command": "PORT=8080 npm start"
}
```

### 3. 交互式命令

某些需要用户输入的命令也可以配置：

```json
{
  "label": "$(git-commit) Git 提交",
  "description": "交互式 Git 提交",
  "command": "git add . && git commit"
}
```

执行后会在终端中等待你输入提交信息。

### 4. 使用别名简化命令

```json
{
  "label": "$(zap) 快速重启",
  "description": "停止并重新启动服务",
  "command": "pkill -f 'npm start' && npm start"
}
```

### 5. 条件执行

```json
{
  "label": "$(check-all) 智能构建",
  "description": "检查后再构建",
  "command": "npm run lint && npm run test && npm run build"
}
```

如果 lint 或 test 失败，build 不会执行。

---

## 常见问题

### Q1: 没有看到脚本菜单怎么办？

**A**: 首先确保已经配置了脚本。如果没有配置任何脚本，点击终端图标后会提示你打开设置。

### Q2: 如何编辑已配置的脚本？

**A**:
- **全局配置**：按 `Cmd/Ctrl + ,` 打开设置，搜索 `scriptExecutor.scripts`
- **项目配置**：编辑项目根目录下的 `.vscode/settings.json` 文件

### Q3: 脚本执行失败怎么办？

**A**:
1. 检查命令是否正确
2. 确保当前工作目录正确（脚本会在项目根目录执行）
3. 检查是否需要先安装依赖或设置环境变量
4. 在终端中手动运行命令确认是否有效

### Q4: 可以在脚本中使用环境变量吗？

**A**: 可以！示例：

```json
{
  "label": "$(server) 生产环境启动",
  "command": "NODE_ENV=production npm start"
}
```

### Q5: 项目配置会覆盖全局配置吗？

**A**: 不会完全覆盖。两者会合并显示，但如果命令相同，项目配置优先。

### Q6: 支持多行命令吗？

**A**: 支持！使用 `&&` 或 `;` 连接多个命令：

```json
{
  "label": "复杂脚本",
  "command": "cd src && npm install && npm run build"
}
```

---

## 最佳实践

### 1. 合理使用全局和项目配置

- **全局配置**：通用工具命令（Git、Docker、npm 等）
- **项目配置**：项目特定命令（启动、构建、测试等）

### 2. 添加清晰的描述

良好的描述能帮助团队成员快速理解脚本用途：

```json
{
  "label": "$(database) 迁移",
  "description": "运行数据库迁移脚本（需要先启动数据库）",
  "command": "npm run migrate"
}
```

### 3. 使用有意义的图标

选择与功能相关的图标，提高可识别性。

### 4. 团队协作

将项目配置的 `.vscode/settings.json` 提交到 Git 仓库，让团队成员都能使用相同的脚本。

### 5. 定期清理

及时删除不再使用的脚本配置，保持列表简洁。

---

## 更新日志

### v0.0.1
- 初始版本发布
- 支持全局配置和项目配置
- 支持自定义脚本执行
- 支持 VSCode 图标

---

## 反馈与支持

如有问题或建议，欢迎反馈！

---

**祝使用愉快！** 🎉
