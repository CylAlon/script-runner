# Script Executor 安装指南

## 方法一：从本地 .vsix 文件安装（推荐）

### 步骤 1：打包插件

在项目根目录执行以下命令：

```bash
# 如果还没有安装依赖
npm install

# 编译 TypeScript 代码
npm run compile

# 打包插件（会生成 .vsix 文件）
npx vsce package
```

打包成功后，会在项目根目录生成 `script-executor-0.0.1.vsix` 文件。

### 步骤 2：安装插件

有两种方式安装：

#### 方式 A：使用命令行（推荐）

```bash
code --install-extension script-executor-0.0.1.vsix
```

#### 方式 B：通过 VSCode 界面

1. 打开 VSCode
2. 按 `Cmd/Ctrl + Shift + X` 打开扩展面板
3. 点击右上角的 `...` 菜单
4. 选择 **"从 VSIX 安装..."**
5. 选择生成的 `script-executor-0.0.1.vsix` 文件
6. 等待安装完成

### 步骤 3：重新加载 VSCode

安装完成后，VSCode 会提示重新加载窗口。点击 **"重新加载"** 按钮，或者：
- 按 `Cmd/Ctrl + Shift + P` 打开命令面板
- 输入 `Developer: Reload Window` 并回车

---

## 方法二：开发模式测试

如果你想在开发过程中快速测试插件，而不想每次都打包安装：

### 步骤 1：编译代码

```bash
npm run compile
```

或者启用监听模式（代码修改后自动编译）：

```bash
npm run watch
```

### 步骤 2：在 VSCode 中调试

1. 在 VSCode 中打开项目
2. 按 `F5` 或点击菜单 **"运行 > 启动调试"**
3. 会打开一个新的 VSCode 窗口（扩展开发主机）
4. 在新窗口中测试插件功能

这种方式适合开发阶段快速迭代测试。

---

## 验证安装

安装成功后，验证插件是否正常工作：

### 1. 检查插件是否已安装

- 按 `Cmd/Ctrl + Shift + X` 打开扩展面板
- 搜索 "Script Executor"
- 应该能看到已安装的插件

### 2. 配置测试脚本

打开 VSCode 设置（`Cmd/Ctrl + ,`），搜索 `scriptExecutor.scripts`，添加一个测试脚本：

```json
{
  "scriptExecutor.scripts": [
    {
      "label": "$(terminal) 测试脚本",
      "description": "这是一个测试脚本",
      "command": "echo 'Hello from Script Executor!'"
    }
  ]
}
```

### 3. 运行测试脚本

- 点击编辑器右上角的 **终端图标**
- 或按 `Cmd/Ctrl + Shift + P` 打开命令面板，输入 **"打开脚本执行器"**
- 选择 "测试脚本"
- 终端应该输出：`Hello from Script Executor!`

如果以上步骤都正常，说明插件安装成功！

---

## 更新插件

当插件代码有更新时，重新打包并安装：

```bash
# 1. 编译最新代码
npm run compile

# 2. 打包新版本
npx vsce package

# 3. 卸载旧版本（可选）
code --uninstall-extension script-executor

# 4. 安装新版本
code --install-extension script-executor-0.0.1.vsix

# 5. 重新加载 VSCode
```

或者直接安装会自动覆盖旧版本。

---

## 卸载插件

如果需要卸载插件：

### 方式 A：使用命令行

```bash
code --uninstall-extension script-executor
```

### 方式 B：通过 VSCode 界面

1. 按 `Cmd/Ctrl + Shift + X` 打开扩展面板
2. 找到 "Script Executor"
3. 点击齿轮图标
4. 选择 **"卸载"**

---

## 常见问题

### Q1: 打包时提示缺少 vsce 工具

**解决方案**：安装 vsce

```bash
npm install --save-dev @vscode/vsce
```

### Q2: 打包时提示 TypeScript 编译错误

**解决方案**：检查并修复 TypeScript 错误

```bash
npm run compile
```

查看错误信息并修复后重新打包。

### Q3: 安装后在扩展列表中找不到插件

**解决方案**：
1. 重新加载 VSCode 窗口
2. 检查是否安装成功：`code --list-extensions | grep script`
3. 如果没有，重新安装一次

### Q4: 点击终端图标没有反应

**解决方案**：
1. 打开 VSCode 开发者工具：`Cmd/Ctrl + Shift + I`
2. 查看控制台是否有错误信息
3. 确保已配置至少一个脚本
4. 重新加载窗口

### Q5: 修改配置后不生效

**解决方案**：
- 保存配置文件
- 重新打开脚本选择器
- 配置会实时生效，无需重启 VSCode

---

## 分发插件

如果要分享给其他人使用：

### 方式 1：分享 .vsix 文件

直接把生成的 `script-executor-0.0.1.vsix` 文件发送给其他人，他们使用上述安装方法安装即可。

### 方式 2：发布到 VSCode 市场

如果想发布到官方市场，需要：

1. 注册 Azure DevOps 账号
2. 创建发布者账号
3. 使用 `vsce publish` 命令发布

详细步骤参考：[VSCode 扩展发布文档](https://code.visualstudio.com/api/working-with-extensions/publishing-extension)

---

## 开发调试技巧

### 查看插件日志

打开 VSCode 输出面板：
- 按 `Cmd/Ctrl + Shift + U`
- 在下拉菜单中选择 "扩展主机"
- 可以看到插件的 `console.log` 输出

### 监听模式开发

```bash
# 终端 1：启动监听编译
npm run watch

# 终端 2：不需要操作，修改代码会自动编译

# VSCode：按 F5 启动调试
# 在扩展开发主机窗口中测试
# 修改代码后，按 Cmd/Ctrl + R 重新加载测试窗口
```

---

**祝你使用愉快！** 🎉

有问题欢迎反馈。
